public Boolean isPrime(Integer num) {
    //code 
    if(num == null){
        return null;
    }
    if (Math.mod(num,2) == 0 || Math.mod(num,5)==0 || Math.mod(num,3)==0){
        return true;
    }else{
        return false;
    }
}