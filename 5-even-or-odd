public String evenOrOdd(Integer num) {
    //code here
    if(num == null)
    return null;

    Integer temp = Math.abs(num);
    if(Math.mod(temp,2) == 0)
        {
            return 'even';
        }
        else{
            return 'odd';
        }
}